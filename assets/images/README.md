# Legacy asset folder

**Brand logo:** use the project root folder instead:

`smartinstaller_AI/images/logo.png`

The desktop UI does not load logos from this `assets/images/` directory. SVG files here are unused placeholders and may be removed.
